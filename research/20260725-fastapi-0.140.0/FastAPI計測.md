# FastAPI 0.140.0でメモリ効率が改善されたらしいので試してみる。

## はじめに
2026年7月25日 FastAPI 0.140.0にてメモリ効率が改善されたとのこと。

## 公式で何が変わったか

リリース: [0.140.0](https://github.com/fastapi/fastapi/releases/tag/0.140.0)（2026-07-24）

| 項目 | 内容 |
|------|------|
| コミット | [`0270829`](https://github.com/fastapi/fastapi/commit/0270829) — Reduce memory usage in dependencies |
| PR | [#16049](https://github.com/fastapi/fastapi/pull/16049) |
| 対象 | `fastapi/dependencies/models.py`, `utils.py`, `routing.py` など |
| 背景 | [Discussion #14742](https://github.com/fastapi/fastapi/discussions/14742)。`Dependant` のキャッシュ属性を整理し、クラスはデータ保持に寄せる |
| 関連 | CI メモリベンチ追加 [#16046](https://github.com/fastapi/fastapi/pull/16046) |

比較相手は直前版 **0.139.2**。

> 補足: Rust による JSON シリアライズ高速化は **0.130.0**（[#14962](https://github.com/fastapi/fastapi/pull/14962)）で入っているため、これ以前のバージョンの人はさらに高速化が体感できそう。  

## 環境
試してみる。

| 項目 | 値 |
|------|-----|
| OS | Windows 11 / WSL2 Ubuntu 24.04 |
| CPU | Intel Core i7-10700F |
| メモリ | 64GB DDR4 |
| アプリ | `develop/api`（Docker + uv + Python 3.14） |
| 計測ツール | wrk `debian/4.1.0`、`docker stats` |
| baseline | FastAPI **0.139.2** |
| 更新後 | FastAPI **0.140.0** |

## 1. 効果が出そうな処理を生やす

0.140.0 の改善対象は **ネストした `Depends` の Dependant グラフ**。巨大 JSON レスポンスより、深い dependency ツリーの方が差が出やすい。

実装場所: `develop/api/app/features/deps/`

| 定数 | 値 | 意味 |
|------|-----|------|
| `CHAIN_DEPTH` | 24 | チェーン深さ |
| `FANOUT_WIDTH` | 8 | 各段の扇出し幅 |
| `ROUTE_COUNT` | 40 | 同一グラフを参照するミラールート数 |

エンドポイント:

- `GET /deps` … メイン計測用（OpenAPI に表示）
- `GET /deps/r0` … `r39` … グラフ複製用（スキーマ非表示）

## 2. 起動とバージョン確認

```bash
cd develop/api
docker compose up -d --build

# 依存グラフ構築で起動に十数秒かかることがある
curl -s http://127.0.0.1:8000/health
curl -s http://127.0.0.1:8000/deps
# {"depth":24,"width":8,"route_count":40,"leaf_sum":15390,"chain_tip":15390}

docker compose exec api python -c "import fastapi; print(fastapi.__version__)"
```

baseline にするとき:

```bash
uv add "fastapi==0.139.2"
docker compose up -d --build
```

0.140.0 にするとき:

```bash
uv add "fastapi==0.140.0"
docker compose up -d --build
```

## 3. wrk の準備

```bash
sudo apt-get update && sudo apt-get install -y wrk
wrk --version
# wrk debian/4.1.0-...

mkdir -p develop/api/bench
cd develop/api
```

## 4. メモリ計測（アイドル）

CPU が落ち着いてから（`< 2%`）取る。起動直後の高 CPU 中は除外。

```bash
# api-api-1 だけ見る
for i in $(seq 1 5); do
  date -Iseconds
  docker stats --no-stream --format "{{.Name}} {{.CPUPerc}} {{.MemUsage}}" | grep '^api-api-1 '
  sleep 1
done | tee bench/mem_idle_<version>.txt
```

## 5. wrk 計測（負荷）

```bash
# ウォームアップ
curl -s -o /dev/null -w "%{http_code} %{time_total}s\n" http://127.0.0.1:8000/deps

# 本計測（別ターミナルで docker stats も同時取得してよい）
wrk -t4 -c20 -d30s --latency \
  "http://127.0.0.1:8000/deps" \
  | tee bench/wrk_<version>.txt
```

負荷中メモリ:

```bash
for i in $(seq 1 35); do
  date -Iseconds
  docker stats --no-stream --format "{{.Name}} {{.CPUPerc}} {{.MemUsage}}" | grep '^api-api-1 '
  sleep 1
done | tee bench/mem_<version>.txt
```

### 失敗例（参考）— 削除済み `/items` を叩いた場合

以前の巨大 JSON 用エンドポイントは削除済み。誤って叩くと **全レスポンスが Non-2xx** になり、スループット数字は無効。

```text
wrk ... "http://127.0.0.1:8000/items?n=2000&description_repeat=20"
...
Non-2xx or 3xx responses: 46594   # ← 全部エラー。計測として使えない
```

必ず `GET /deps` を使う。

---

## 計測結果（2026-07-25 実測）

生ログ: `develop/api/bench/`

| ファイル | 内容 |
|----------|------|
| `wrk_0.139.2.txt` / `wrk_0.140.0.txt` | wrk |
| `mem_idle_0.139.2.txt` / `mem_idle_0.140.0.txt` | アイドル RSS |
| `mem_0.139.2.txt` / `mem_0.140.0.txt` | 負荷中 RSS |

### アイドルメモリ（CPU < 2% で採取・採用値）

| 版 | CPU | MemUsage |
|----|-----|----------|
| **0.139.2** | 0.15–0.19% | **1.581 GiB**（≈ 1619 MiB） |
| **0.140.0** | 0.13–0.21% | **771.5 MiB** |

0.139.2（抜粋）:

```text
api-api-1 0.16% 1.581GiB / 47.04GiB
api-api-1 0.15% 1.581GiB / 47.04GiB
api-api-1 0.19% 1.581GiB / 47.04GiB
```

0.140.0（抜粋）:

```text
api-api-1 0.17% 771.5MiB / 47.04GiB
api-api-1 0.13% 771.5MiB / 47.04GiB
api-api-1 0.21% 771.5MiB / 47.04GiB
```

### wrk（`GET /deps`, `-t4 -c20 -d30s`・採用値）

| 指標 | 0.139.2 | 0.140.0 |
|------|---------|---------|
| Latency avg | 615.16 ms | 953.57 ms |
| Latency p50 | 614.93 ms | 953.75 ms |
| Latency p99 | 676.66 ms | 1.05 s |
| Requests/sec | 34.28 | 20.72 |
| 総リクエスト | 965 | 584 |
| timeout | 20 | 40 |
| Non-2xx | 0 | 0 |

---

## 改善率比較（0.139.2 → 0.140.0）

計算:

- メモリ削減率 = `(旧 − 新) / 旧 × 100`（大きいほど改善）
- Latency / timeout 変化率 = `(新 − 旧) / 旧 × 100`（負なら短縮＝改善）
- RPS / 総リクエスト変化率 = `(新 − 旧) / 旧 × 100`（正なら改善）

### サマリ表

| 指標 | 0.139.2（旧） | 0.140.0（新） | 差分 | 変化率 | 評価 |
|------|---------------|---------------|------|--------|------|
| アイドル RSS | 1.581 GiB（1619 MiB） | 771.5 MiB | **−847 MiB** | **−52.3%**（使用量） | **改善**（約半減） |
| Latency avg | 615.16 ms | 953.57 ms | +338.41 ms | +55.0% | 悪化 |
| Latency p50 | 614.93 ms | 953.75 ms | +338.82 ms | +55.1% | 悪化 |
| Latency p99 | 676.66 ms | 1.05 s | +約 373 ms | +55.2% | 悪化 |
| Requests/sec | 34.28 | 20.72 | −13.56 | −39.6% | 悪化 |
| 総リクエスト（30s） | 965 | 584 | −381 | −39.5% | 悪化 |
| timeout | 20 | 40 | +20 | +100% | 悪化 |

### メモリ改善の内訳

| 項目 | 値 |
|------|-----|
| 旧（0.139.2） | 1.581 × 1024 = **1618.9 MiB** |
| 新（0.140.0） | **771.5 MiB** |
| 削減量 | 1618.9 − 771.5 = **847.4 MiB** |
| 削減率 | 847.4 / 1618.9 ≈ **52.3%** |
| 新 / 旧 | 771.5 / 1618.9 ≈ **47.7%**（旧の半分弱） |

### 読み方

| 目的 | 結果 |
|------|------|
| dependency メモリ削減（#16049 の狙い） | **52.3% 改善**を確認 |
| スループット / レイテンシ | 今回の `/deps` 負荷では改善なし（RPS −39.6%、Latency +55%） |

---

## 再計測（2026-07-25 18:10 頃・0.140.0 再更新）

「本当に wrk が悪化しているのか」を確認するため、再度 `fastapi==0.140.0` に上げて計測した。

手順:

1. `uv add "fastapi==0.140.0"` → `docker compose up -d --build`
2. `/deps` が 200、CPU &lt; 2% まで待機
3. idle メモリ 5 サンプル
4. wrk を **2 回**連続（間に 10s cooldown）

生ログ:

| ファイル | 内容 |
|----------|------|
| `bench/mem_idle_0.140.0_retest.txt` | 再計測 idle |
| `bench/wrk_0.140.0_retest1.txt` | wrk 1 回目 |
| `bench/wrk_0.140.0_retest2.txt` | wrk 2 回目 |

### 再計測: アイドルメモリ

| サンプル | CPU | MemUsage |
|----------|-----|----------|
| 1 | 0.15% | 772.5 MiB |
| 2 | 0.18% | 772.1 MiB |
| 3 | 0.14% | 772.1 MiB |
| 4 | 0.18% | 771.8 MiB |
| 5 | 0.20% | 771.6 MiB |
| **代表値** | — | **≈ 772 MiB** |

前回の 0.140.0 idle（771.5 MiB）と一致。メモリ改善は再現。

### 再計測: wrk（2 回）

| 回 | Latency avg | p50 | p99 | Req/sec | 総req | timeout |
|----|-------------|-----|-----|-------------|-------|---------|
| retest1 | 977.93 ms | 984.42 ms | 1.04 s | **20.62** | 580 | 40 |
| retest2 | 970.41 ms | 976.99 ms | 1.03 s | **19.13** | 540 | 60 |
| **平均** | **≈ 974 ms** | **≈ 981 ms** | **≈ 1.04 s** | **≈ 19.9** | **≈ 560** | **50** |

### 0.139.2 との再比較（再計測平均）

| 指標 | 0.139.2 | 0.140.0 再計測（平均） | 変化率 | 評価 |
|------|---------|------------------------|--------|------|
| アイドル RSS | 1.581 GiB（1619 MiB） | ≈ 772 MiB | **−52.3%** | **改善**（再現） |
| Latency avg | 615.16 ms | ≈ 974 ms | **+58.3%** | **悪化**（再現） |
| Latency p50 | 614.93 ms | ≈ 981 ms | **+59.5%** | **悪化**（再現） |
| Requests/sec | 34.28 | ≈ 19.9 | **−42.0%** | **悪化**（再現） |
| timeout | 20 | ≈ 50 | **+150%** | **悪化**（再現） |

### 再計測の結論

| 疑問 | 答え |
|------|------|
| メモリ削減は本物か | **はい。** 772 MiB 前後で再確認（旧 1.581 GiB から約半減） |
| wrk 悪化は計測ミスか | **いいえ。** 2 回とも RPS ≈ 20 / Latency ≈ 970 ms で、旧（RPS 34 / Latency 615 ms）より明確に遅い |

悪化の理由候補（推測）:

- #16049 は **Dependant のメモリ構造最適化**であり、リクエストごとの解決速度を上げる変更ではない
- 本ベンチの `/deps` は極端に深い dependency グラフで、解決コストが支配的
- timeout 増加も、1 リクエストが wrk の待ち時間に収まりにくいことの表れ

---

## 考察

1. **メモリ削減は明確**  
   ネスト `Depends` を大量に載せたアプリで、アイドル RSS が 1.581 GiB → 771.5 MiB（**52.3% 削減**）。公式 PR の狙い（dependency メモリ）と一致する。

2. **今回の wrk ではスループットは上がっていない**  
   0.140.0 の方が Latency が長く（+55%）、RPS は下がった（−39.6%）。#16049 はメモリ最適化であり、この極端な dependency グラフの解決速度まで保証するものではない。timeout も増えている。

3. **計測のポイント**  
   - エンドポイントは `/deps`
   - アイドルメモリは CPU が落ち着いてから  
   - `docker stats` は対象コンテナ名で絞る
   - 速度比較は複数回 wrk してばらつきを見る

## 結論

| 見たいもの | 結果 |
|------------|------|
| 0.140.0 の dependency メモリ改善 | **再現できた**（アイドル RSS **52.3% 削減**） |
| wrk の RPS / latency 改善 | **今回の負荷では観測されず**（悪化） |

「メモリを食う Depends だらけの API」を抱えているなら 0.140.0 への更新は効きやすい。スループット改善を期待するなら、別指標・別負荷（例: 0.130.0 前後の巨大 JSON）で測る必要がある。

---

## なぜ悪化したか

### 悪化した原因：#16049 によるトレードオフ

一言でいうと、**0.140.0 はメモリを減らすためにキャッシュを捨て、その分だけリクエスト処理が重くなった**。

これまで（0.139.2）FastAPI は、`Depends` を解析した結果（「この関数は async か？」「必要な scope は？」など）を**一度計算したら覚えておく**仕組みだった（`@cached_property`）。速いが、覚えておくぶんメモリを食う。

0.140.0（[#16049](https://github.com/fastapi/fastapi/pull/16049)）は、この「覚えておく」をやめた。

| | 0.139.2（旧） | 0.140.0（新） |
|---|---|---|
| Depends の解析結果 | **1回計算して保持** | **毎リクエスト計算し直す** |
| メモリ | 多い（保持するため） | **少ない（−52%）** |
| リクエストの重さ | 軽い（計算済み） | **重い（毎回計算）** |

つまり **「メモリ ↔ 速度」の交換**。開発者本人も PR で「キャッシュはそれほど有用でないので消した（the cache not being that useful）」と説明している。メモリ半減はこの狙い通りで、速度低下はその裏返し。

### なぜ今回はこんなに遅くなったのか

「毎回計算し直す」コストは、**Depends のネストが深いほど膨らむ**。今回のベンチはそこを極端にした設定:

- `CHAIN_DEPTH=24`（24段のネスト）・`FANOUT_WIDTH=8`・ルート40本
- → 1リクエストで数千ノードを毎回解析し直す ＝ **Latency +55%**

**つまり「捨てたキャッシュが一番効いていた形」をわざと作ったベンチ**。改善の悪い面だけが最大化されて出た数字であり、一般的な API には当てはまらない。

### 補足：RPS と Latency は「別々の悪化」ではない

今回は接続数を固定した負荷（`-c20`）なので、`RPS ≒ 接続数 ÷ Latency` の関係で決まる。

- 0.139.2: 20 ÷ 0.615s ≒ 34（実測 34.3）
- 0.140.0: 20 ÷ 0.953s ≒ 21（実測 20.7）

RPS 低下と Latency 増加は**同じ事実の裏表**。「1リクエストが重くなった」の一点に集約される（uvicorn 1プロセス＝実質1コアで捌いているのも効いている）。

## 実運用で起こる可能性があるか

**ほぼ心配いらない。**

- 普通の API は Depends のネストが浅い（数段程度）。毎回計算し直しても誤差レベルで、速度差は体感できない。
- 今回のような24段ネストは現実では作らない（設計として過剰）。
- 逆に **メモリ削減（−52%）は多くのアプリで効く**。特に Depends を多用する大きめのアプリほど恩恵が大きい。

→ ほとんどの環境で **0.140.0 へのアップデートはプラス**。速度が落ちて見えるのは、今回のような極端なネストを組んだ特殊ケースだけ。

## まとめ

- 遅くなった原因は **#16049 のメモリ↔速度トレードオフ**（キャッシュ廃止でメモリ半減、代わりに毎回計算で重くなった）。
- 今回の数字は **深いネスト Depends を意図的に作った病的ベンチ**なので大きく出ただけ。RPS と Latency の悪化は同じ現象。
- **実運用ではほぼ問題にならず、メモリ削減の恩恵の方が大きい。**
