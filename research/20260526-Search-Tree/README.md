# 🧪探索木のアルゴリズムを実際にPythonで試してみる。

## 環境

このディレクトリは **独立した uv プロジェクト**（Python 3.14）。  
リポジトリ方針: [research/README.md](../README.md)

```bash
cd research/20260526-Search-Tree
uv sync
uv run jupyter lab notebooks/8-puzzle-search-tree.ipynb
```

実装: [notebooks/8-puzzle-search-tree.ipynb](./notebooks/8-puzzle-search-tree.ipynb)

## ナンプレ（数独）ソルバー

CLI スクリプト: [sudoku_solver.py](./sudoku_solver.py)

8パズルノートブックと同様、**探索木 + 深さ優先探索（DFS）** で解く。空きマスに 1〜9 を仮置きし、行・列・3×3 ブロックの制約に違反したら **バックトラック** して別の枝を試す。

- **状態**: 9×9 盤面（`0` が空きマス）
- **枝**: 空きマスへの数字の仮置き
- **枝刈り**: 行・列・ブロックに既に使われている数字は候補から除外
- **ヒューリスティック**: 候補数が最少の空きマス（MRV）を先に埋め、探索の分岐を抑える

### 入出力

| 項目 | 形式 |
|------|------|
| 入力 | 81 文字の文字列。行優先で 9×9。`0`〜`9` のみ。空きマスは `0` |
| 出力 | 入力盤面と、解けた場合は完成盤面（空きマスは `.` 表示） |

### 実行

プロジェクト直下で `uv sync` 済みであること。

```bash
cd research/20260526-Search-Tree
uv run python sudoku_solver.py "530070000600195000098000060800060003400803001700020006060000280000419005000080079"
```

リポジトリルートから実行する場合:

```bash
uv run --project research/20260526-Search-Tree python research/20260526-Search-Tree/sudoku_solver.py "530070000600195000098000060800060003400803001700020006060000280000419005000080079"
```

解なしの場合は `この問題は解けませんでした（解なし）。` と表示する。

---

"""
==============================================================================
探索木（Search Tree）のアルゴリズム基礎
==============================================================================

【概要】
探索木とは、問題の解決策を見つけるために、取り得るすべての状態や選択肢を
木の枝分かれ（ツリー構造）のように図式化し、正解への経路を探し出す手法です。
AIの初期（第1次AIブーム）における「推論・探索」の代表的なアプローチであり、
迷路の攻略やパズル（トイ・プロブレム）を解く際によく用いられます。

探索を進めるアルゴリズムには、大きく分けて以下の2つのアプローチがあります。

------------------------------------------------------------------------------
1. 深さ優先探索（DFS: Depth-First Search）
------------------------------------------------------------------------------
【仕組み】
スタート地点からある枝（選択肢）を選んだら、行き止まりになるまでひたすら深く
掘り進めます。行き止まりに到達したら、一つ前の分岐点まで戻り（バックトラック）、
まだ探索していない別の枝を探索します。これをゴールが見つかるまで繰り返します。

【特徴と実装のポイント】
- メリット: 現在探索している経路の情報だけを記憶すればよいため、メモリ消費が少ない。
- デメリット: ゴールは見つけられるが、それが「最短経路」である保証はない。
- 実装時のデータ構造: 「スタック（Stack：後入れ先出し）」を使う。
  （※Pythonでは通常のリストの append() と pop() で簡単に実装可能）

------------------------------------------------------------------------------
2. 幅優先探索（BFS: Breadth-First Search）
------------------------------------------------------------------------------
【仕組み】
スタート地点から近い深さ（階層）にあるノード（選択肢）を、横一列にすべて順番に
調べていきます。現在の階層をすべて調べ終わってから、次の深い階層へと進みます。
水面に石を投げた時の「波紋」が広がっていくようなイメージで探索を進めます。

【特徴と実装のポイント】
- メリット: ゴールが存在する場合、必ず「最短経路」を最初に見つけ出すことができる。
- デメリット: 各階層のすべての分岐を記憶しながら進むため、階層が深くなったり
  選択肢が多いと、メモリの消費量が爆発的に増える。
- 実装時のデータ構造: 「キュー（Queue：先入れ先出し）」を使う。
  （※Pythonでは collectionsモジュールの deque を使うのが一般的）

==============================================================================
"""

# ----------------------------------------------------------------------------
# 以下は、実際にPythonで探索木を実装する際の基本的な処理の流れ（疑似コード）
# ----------------------------------------------------------------------------

# 1. 探索待ちのノードを入れるリスト（スタック または キュー）を初期化する
# 2. 探索済みのノードを記録するリスト（またはSet）を用意する
# 3. スタート地点のノードを探索待ちリストに入れる

# 4. 探索待ちリストが空になるまで以下のループを繰り返す:
#    a. リストから次に探索するノードを1つ取り出す
#       - DFSの場合: リストの「最後」に追加されたものを取り出す（pop）
#       - BFSの場合: リストの「最初」に追加されたものを取り出す（popleft）
#
#    b. 取り出したノードがゴールかどうかを判定する
#       - ゴールなら探索終了（正解の経路を返す）
#
#    c. ゴールでない場合、そのノードがまだ「探索済み」でなければ:
#       - 探索済みリストに記録する
#       - そのノードから次に行けるすべてのノード（子ノード）を取得し、
#         探索待ちリストに追加する

# 5. ループが終わってもゴールが見つからなければ、正解なし（到達不可能）